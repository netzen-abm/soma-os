Archived standalone integration-test draft from the Issue #50 implementation branch. The active integration coverage is consolidated into legacy_promotion.rs so the production module and its PostgreSQL tests share one canonical compilation path.

Original source was preserved in commit history before removal from the active source tree.